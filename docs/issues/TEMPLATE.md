# STEP-NN：<标题>

- 线:A（前端三栏）/ B（去 Docker）/ 基线
- 状态:⬜ 未开始 / 🟡 进行中 / ✅ 已合 / 🔴 受阻
- 依赖:STEP-XX（前置）
- 对应 PR:<链接或分支名 `codex/<topic>`>

## 目标（一句话）

<这一步要达成的唯一结果>

## 改动面

- 文件:<列出会动的文件>
- 不动:<明确不碰的范围，防止 scope 蔓延>
- AGENTS 连带项:<若碰 MCP/REST/版本，列出必改的 7~8 处；否则写「无」>

## 结果预测（执行前填）

- 构建:`npm run build` 预期 <通过 / 具体变化>
- 测试:`npm test` 预期 <从 X 失败 → Y 失败 / 新增哪些测试通过>
- 行为:<用户/接口可见的变化>
- 风险:<最可能出错的地方 + 征兆>

## 验证命令

```bash
npm run build
npm test -- --run <相关测试文件>
# 其他:check:workbench / 手动点验
```

## 回滚

<如何安全回退:revert 单个 PR / 关 feature flag>

## 实际反馈（执行后由你回填）

- 构建:
- 测试:
- 行为:
- 与预测的差异:
- 下一步影响:
