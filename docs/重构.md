## AI-TODO 项目重构计划

1. 安装/使用方式：需要将整个项目重构成一个 CLI 程序，让用户直接运行。最好是用一个 skill 可以搞定，这样用户可以在 agent 中使用
2. 代码重构
  1. 当前代码是基于开源项目 fork，内容很多，需要分析精简
  2. 考虑从 0 重新开发一版。
3. 前端重构：当前的前端 viewer 部分是几个 parts 拼接成的一个巨大的 html 文件，由一个 node.js server 服务的。需要用 React 技术栈重构。前端技术栈使用：React+Typescript+TailwindCSS+Shadcn/ui，图标库使用 lucide-react
4. UI/UX 优化：
  1. 同时支持 codex + claude code
  2. 安装后首次启动，自动开始分析本地 sessions，并且在页面上增加 loading 遮罩层，提示用户要等待较长时间
  3. Organize 完成之后，需要展示一个弹窗，展示这次分析任务的结果总结，分析了多少个会话的多少条消息内容，用了 LLM 还是规则匹配的模式，中间出了什么问题（比如 LLM 没有连上，没有符合要求的会话）。
  4. 很多文案不合适，待修改。
5. 技术优化：
  1. session 文件分析需要优化，加快分析过程。如 session 文件的遍历逻辑（先检查文件的修改时间），单个大 session 文件可以只看最后 n 行，不需要分析一整个文件。
  2. 与 LLM 交互的流程可能需要优化，加快分析过程
